# ============================================================================
# Credentials Configuration (DO NOT COMMIT TO VERSION CONTROL)
# ============================================================================

# Charles Schwab API
schwab:
  app_key: ""
  app_secret: ""
  redirect_uri: "https://127.0.0.1"
  token_file: "config/schwab_token.json"
  account_number: ""

# Email (Gmail App Password recommended)
email:
  sender_email: ""
  sender_password: ""        # Use App Password, not account password
  recipients:
    - ""

# Anthropic LLM (only needed when llm.provider = "claude")
anthropic:
  api_key: ""
